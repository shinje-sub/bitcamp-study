package com.eomcs.lms.util;

public class ArrayList {

  Object[] elementData;
  int size;

  public ArrayList() {
    super(); 
    // 슈퍼 클래스를 넣지 않으면 기본으로 생성된다.

    this.elementData = new Object[100];
  }

  public ArrayList(int initialCapacity) {
    super();
    this.elementData = new Object[initialCapacity];
  }
}
